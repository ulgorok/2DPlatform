using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TutoGone : MonoBehaviour
{
    public static bool gone;
    // Start is called before the first frame update
    void Awake()
    {
        gone = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
